node_modules/
.next/
.vercel
.env.local
.env
*.log
.DS_Store
.claude/
scripts/profile.mjs

# The profiled dataset embeds a real student profile — the site serves the
# neutral one and evaluates in the browser instead.
public/data/fall2026-triage.json
# Copied from pdfjs-dist on dev/build so the worker is served from this origin.
public/pdf.worker.min.mjs
.env*
